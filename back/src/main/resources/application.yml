server:
  port: ${PORT:8080}
cors:
  allowed-origins: http://localhost:3000/

spring:
  datasource:
    url: jdbc:postgresql://34.64.69.195:5432/sound_of_mind
    username: postgres
    password: 1111
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false
  mvc:
    view:
      prefix: /templates/
      suffix: .html
  web:
    resources:
      static-locations: classpath:/static/

jwt:
  secret: your-secret-key-here-must-be-at-least-256-bits-long
  expiration: 86400000 # 24 hours

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    com.capstone: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE